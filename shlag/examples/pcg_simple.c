// Stub file for compiling example included in header. To build run something like:
// cc -I. examples/pcg_simple.c -o bin/pcg_simple
#define SHLAG_PCG_EXAMPLE
#define SHLAG_PCG_IMPL
#define SHLAG_PCG_DEBUG // optional, for enabling assertions
#define SHLAG_PCG_DEF static // optional, but serves for example, and makes executable smaller
#include "shlag_pcg.h"
